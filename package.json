{
  "scripts": {
    "test": "npm test --prefix server && npm test --prefix mobile",
    "test:coverage": "npm run test:coverage --prefix server && npm run test:coverage --prefix mobile"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.1.1"
  }
}
